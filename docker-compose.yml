services:
  db:
    image: postgres:16
    env_file:
      - .env
    ports:
      - "5432:5432"
  web:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db
    env_file:
      - .env